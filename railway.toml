# railway.toml — deploys the backend to Railway.
 
[build]
builder = "nixpacks"
 
[deploy]
startCommand = "cd backend && npm install && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
 